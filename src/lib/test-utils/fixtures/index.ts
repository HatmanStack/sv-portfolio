/**
 * Central export for all test fixtures
 * Fixtures are static test data based on real production data
 *
 * Note: Fixtures vs Factories
 * - Fixtures are static, pre-defined test data (use for integration-style tests)
 * - Factories are dynamic functions that create test data (use for unit tests with flexibility)
 */

export { projectsFixture } from './projects';
export { navigationItemsFixture } from './navigation';
